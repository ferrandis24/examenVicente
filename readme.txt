Tienes que poner vagrant up y vagrant ssh, una vez hayas hecho esto,estaras dentro de la maquina virtual.
Luego tendras que poner sudo apt-get install apache2, si no te dejara instalarlo, pon el comando sudo apt-get update y luego vuelve a poner sudo apt-get install apache2
